xrd.port {PORT}
oss.localroot {DATA_DIR}
all.export /
all.adminpath {ADMIN_DIR}
all.pidpath   {RUN_DIR}
xrd.trace off
xrootd.seclib {SECLIB}
sec.protocol gsi -certdir:{CA_DIR} -cert:{SERVER_CERT} -key:{SERVER_KEY} -gmapopt:10
acc.authdb {AUTHDB_PATH}
